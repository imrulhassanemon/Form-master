/* 

    * use form tag and onSubmit handeller with event (e).
    * form access e.target.[name of the input field].value.
    * 
    * 
    * 2.....
    * controlled component: a component that does not maintain its own state, but instead relies on props to determine its behavior.
    *
    * 3.....
    * controlled element: one object in a single state holding all the input fields value.

*/